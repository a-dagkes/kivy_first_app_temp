# Kivy — Домашка №1

Простое приложение на Kivy, выполненное в рамках интенсива.

## 📱 О проекте

Приложение состоит из:
* 1 основного экрана-меню
* 4 функциональных экрана:
  * **Экран 1**: Кнопки с изменением цвета
  * **Экран 2**: Ввод пароля с валидацией
  * **Экран 3**: Минутный таймер с прогресс-баром
  * **Экран 4**: Прокручиваемый длинный текст

Проект направлен на закрепление базовой работы с `ScreenManager`, макетами и виджетами.

## 🛠 Особенности реализации

* Создан собственный класс `ScrButton` для переключения между экранами
* Реализован кастомный `ScrollLabel` с интерактивной полосой прокрутки
* Использован таймер с привязкой к `NumericProperty` для автообновления UI
* Реализована логика жизненного цикла экранов (`on_enter`, `on_leave`, `on_pre_enter`)
* Адаптивная ширина поля ввода при изменении размера окна
* Отработана навигация между экранами с анимацией переходов

## 📚 Что используется

* `ScreenManager` и `Screen` для многоэкранной навигации
* `BoxLayout` для компоновки интерфейса
* `ScrollView` с кастомной обработкой touch-событий
* `Clock` для таймера и периодических обновлений
* `NumericProperty` для реактивного обновления прогресс-бара
* `ProgressBar` для визуализации таймера
* `TextInput` для ввода пароля
* Кастомные классы виджетов

## 🚀 Запуск
```bash
pip install -r requirements.txt
python cuatroscr_app.py
```

## 📝 Требования

* Python 3.8+
* Kivy 2.3.0+

## 📄 Структура проекта
```
.
├── cuatroscr_app.py  # Основной файл приложения
├── requirements.txt  # Зависимости проекта
└── README.md         # Файл описания и инструкций
```

##

Aia D, June developer

**Учебный проект курса Python-Интенсив**
